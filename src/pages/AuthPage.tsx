
import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { useStore } from '../store/useStore';
export function AuthPage(){const [mode,setMode]=useState<'login'|'register'>('login');const [email,setEmail]=useState('admin@laptopstore.vn');const [password,setPassword]=useState('123456');const {login,addToast}=useStore();const nav=useNavigate();const submit=(e:React.FormEvent)=>{e.preventDefault();if(mode==='register'){addToast({type:'success',title:'Đăng ký demo thành công',message:'Dùng tài khoản user mẫu để đăng nhập'});setMode('login');return}if(login(email,password)){addToast({type:'success',title:'Đăng nhập thành công'});nav(email.includes('admin')?'/admin':'/')}else addToast({type:'error',title:'Sai thông tin đăng nhập',message:'Thử admin@laptopstore.vn / 123456'})};return <main className="container-page grid min-h-[70vh] place-items-center py-10"><form onSubmit={submit} className="card w-full max-w-md p-8"><h1 className="text-3xl font-black">{mode==='login'?'Đăng nhập':'Đăng ký'}</h1><p className="mt-2 text-slate-600">Admin: admin@laptopstore.vn / 123456<br/>User: user@laptopstore.vn / 123456</p><label className="mt-5 block font-black">Email<input className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-4 block font-black">Mật khẩu<input type="password" className="input mt-2" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn btn-primary mt-6 w-full">{mode==='login'?'Đăng nhập':'Tạo tài khoản demo'}</button><button type="button" className="mt-4 w-full font-bold text-brand-600" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Chưa có tài khoản? Đăng ký':'Đã có tài khoản? Đăng nhập'}</button></form></main>}
